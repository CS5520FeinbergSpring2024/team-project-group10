using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "BuildMenuState", menuName = "ScriptableObjects/BuildMenuState", order = 1)]
public class BuildMenuScriptableObject : ScriptableObject
{
    public bool isOpen = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
